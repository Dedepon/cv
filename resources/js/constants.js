// -----------
// Constants -
// -----------

const applicationHeight = document.body.clientHeight;
const applicationWidth = document.body.clientWidth;
const catMovement = 8
const backgroundAndCloudMovement = -0.6
const backgroundBaseMovement = 0;
const cloudsBaseMovement = -0.2;
const midgroundBaseMovement = 0;
const midgroundMovement = -1.5;
const foregroundBaseMovement = 0;
const foregroundMovement = catMovement * (-1);